kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: minifiles
nodes:
  - role: control-plane
    extraPortMappings:
      # control plane API reachable at localhost:8471 from the host
      - containerPort: 30080
        hostPort: 8471
        protocol: TCP
