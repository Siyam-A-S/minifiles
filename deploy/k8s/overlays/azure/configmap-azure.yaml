apiVersion: v1
kind: ConfigMap
metadata:
  name: control-plane-config
  namespace: minifiles
data:
  MINIFILES_PROVISIONER: "kubernetes"
  MINIFILES_TIERING_ENABLED: "true"
  MINIFILES_STORAGE_CLASS: "managed-csi"
  # data-plane images come from config (not pod specs), so the deploy
  # workflow bumps these tags too
  MINIFILES_NFS_IMAGE: "minifilesacrxw2sijbw.azurecr.io/minifiles/nfs-godzilla:ed6d47b5e14640a778e5f572375d9063102ff9d4"
  MINIFILES_TIERING_IMAGE: "minifilesacrxw2sijbw.azurecr.io/minifiles/tiering-engine:ed6d47b5e14640a778e5f572375d9063102ff9d4"
  MINIFILES_PUSHGATEWAY_URL: "http://pushgateway-prometheus-pushgateway.monitoring.svc:9091"
