# AKS overlay, synced by Argo CD. The image tags below are bumped by the
# deploy workflow (sed to the pushed git sha) — do not edit tags by hand.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - servicemonitor.yaml
  - grafana-dashboard.yaml
patches:
  - path: configmap-azure.yaml
  - path: service-lb.yaml
images:
  - name: minifiles/control-plane
    newName: minifilesacrxw2sijbw.azurecr.io/minifiles/control-plane
    newTag: initial
